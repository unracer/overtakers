<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.overtakers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Black.NoTitleBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ViewChats"
            android:label="ViewChats">
        </activity>

        <activity android:name=".ViewChat"
            android:label="ViewChatPrivate">
        </activity>

        <activity android:name=".ViewNews"
            android:label="ViewNews">
        </activity>

        <activity android:name=".ViewServices"
            android:label="ViewServices">
        </activity>
    </application>

</manifest>